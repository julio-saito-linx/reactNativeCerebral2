import {
  StyleSheet
} from 'react-native'

export default StyleSheet.create({
  blocksRowsContainer: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'flex-start',
    // flexGrow: 1,
    margin: 0
  },

  blocksContainer: {
    // flex: 1,
    flexDirection: 'row',
    // justifyContent: 'center',
    // alignItems: 'center',
    // flexGrow: 1,
    margin: 0
  }
})
