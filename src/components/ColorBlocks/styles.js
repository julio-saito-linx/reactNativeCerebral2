import {
  StyleSheet
} from 'react-native'

export default StyleSheet.create({
  blocksContainer: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    flexGrow: 1,
    margin: 0
  }
})
