import {
  StyleSheet
} from 'react-native'

export default StyleSheet.create({
  blocksRowsContainer: {
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'flex-start',
    margin: 0
  },

  blocksContainer: {
    flexDirection: 'row',
    margin: 0
  }
})
